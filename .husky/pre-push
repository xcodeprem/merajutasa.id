#!/usr/bin/env sh
. "$(dirname "$0")/_/husky.sh"

echo "[pre-push] Running governance:verify (Wave 0 fail-fast)"
npm run --silent governance:verify || { echo "[pre-push] governance:verify failed – aborting push"; exit 1; }

echo "[pre-push] Success"
