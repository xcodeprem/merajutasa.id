# Node dependencies
node_modules/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
logs/
*.log

# Build / dist outputs (none generated yet, placeholder patterns)
dist/
build/
coverage/
out/
.tmp/
.temp/
.cache/

# Additional build artifacts
*.tgz
*.tar.gz
*.zip
bundle/
compiled/
release/
public/dist/

# Framework caches (future-proof)
.next/

# OS / Editor noise
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp

# Environment & secrets (.env.example may be committed intentionally)
.env
.env.*
!.env.example

# Secret files and keys - CRITICAL SECURITY PATTERNS
*.pem
*.key
*.crt
*.cer
*.p12
*.pfx
*.jks
*.keystore
*.pub
id_rsa*
id_dsa*
id_ecdsa*
id_ed25519*
*.gpg
*.asc

# Cloud and API credentials
*credentials*
*secret*
!*secret*.test.js
!*secret*.js
*token*
.aws/
.azure/
.gcp/
.docker/config.json

# SSH and certificates
.ssh/
authorized_keys
known_hosts

# Database connection strings and configs
*.db
*.sqlite
*.sqlite3
database.yml
database.json

# Secret management files
secrets.yml
secrets.yaml
secrets.json
vault.json
.vault-token

# Cryptographic keys and certificates (NEVER commit)
*.pem
*.key
*.crt
*.p12
*.pfx
*.keystore
*.jks
.pki/
.ssh/
id_rsa*
id_dsa*
id_ecdsa*
id_ed25519*
*.rsa
*.dsa
*.ecdsa
*.ed25519

# Additional secret file patterns
secrets/
secret.*
*.secret
*.token
*.credentials
.credentials/

# SSH keys and authentication files  
ssh_key*
*_key
auth.*
authentication.*

# AWS and cloud config files with potential secrets
aws-config.*
*-config.yml
*-config.yaml
.aws-*

# Package artifacts (moved to build section above for consolidation)
# *.tgz

# Governance Artifacts Notice:
# Do NOT ignore artifacts/ directory: evidence & integrity JSON meant to be committed post-seal.
# If specific large transient files appear later, add targeted patterns instead of excluding the whole folder.

# Volatile logs and debugging artifacts (uploaded as GitHub Actions artifacts)
artifacts/logs/
artifacts/debug/
artifacts/volatile/

# Local integrity keys (private material – never commit)
.integrity/

# Generated dependency diagrams (source .mmd files are committed, generated images are artifacts)
*.png
*.svg
docs/architecture/*.png
# Allow dependency-graph.svg for cross-team consumption (but exclude other SVGs)
!docs/architecture/dependency-graph.svg
build/deps/*.png
build/deps/*.svg
